# NH FAQ Bot

* `/faq <title>` - autocompletes, channel/role-restricted access
* `/refresh-faq` - to rebuild cache on faq update

## Setup

Discord Bot needs `bot`, `application.commands`, `send messages` and `use slash commands`.

```bash
npm install
npm run deploy-commands
npm start
```